{
  "name": "vite-complex-build-poc",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "apps:start": "pnpm --parallel --filter './apps/*' start",
    "apps:build": "pnpm --parallel --filter './apps/*' run build",
    "libs:build": "pnpm --parallel --filter './libs/*' run build",
    "typeCheck": "pnpm --parallel exec tsc --noEmit"
  },
  "author": "craigmiller160",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
