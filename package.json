{
  "name": "vite-complex-build-poc",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "start": "pnpm --parallel start",
    "fullStart": "pnpm --parallel fullStart",
    "build": "pnpm --parallel build",
    "typeCheck": "pnpm --parallel exec tsc --noEmit"
  },
  "author": "craigmiller160",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
